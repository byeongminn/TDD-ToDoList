import styled from 'styled-components';

export const Header = () => {
  return (
    <Container>
      <Title>할 일 목록</Title>
    </Container>
  );
};

const Container = styled.div``;

const Title = styled.div``;
